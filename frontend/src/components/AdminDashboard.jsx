import React from 'react';

const AdminDashboard = () => {
    return (
        <div style={{ background: '#fff', padding: 20, borderRadius: 8 }}>
            <h2>Admin Dashboard (stub)</h2>
            <p>No backend data in this demo.</p>
        </div>
    );
};

export default AdminDashboard;